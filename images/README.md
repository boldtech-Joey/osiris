# images

Photographs used on the site.

- `bench-cover.avif` — cover for The Bench (`bench.html`). Displayed at 16:9 on
  desktop and 4:3 on mobile, cropped from the centre, so keep the subject off
  the extreme edges. Replace this file in place to change the cover; keep the
  name and update the `width`/`height` on the `<img>` if the ratio changes.
